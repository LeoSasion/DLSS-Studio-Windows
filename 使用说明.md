# DLSS Studio

双击 `start-web.bat`，打开 http://127.0.0.1:7860/。重复启动会打开已有服务。

右上角切换深色、浅色主题，浏览器记住选择。上传素材，选择尺寸与风格，点击“开始处理”。图片处理成功后可切换原图、增强后、拖动对比；结果可下载。视频在原片与结果间切换，完整导出与浏览器预览分开保存。

视频的运动、编码和音频选项在右侧折叠设置内。当前环境驱动 591.86 可运行已测试的图片和视频神经渲染；安装包的 NVENC 编码要求更新驱动，程序会检查是否可用，不可用时默认选择 ProRes CPU 编码。ProRes 文件体积较大，可按需要选择 AV1 CPU 等格式。

上传文件位于 `app/uploads/`，结果位于 `app/ui_out/<任务编号>/`。页面只列本次会话中的素材；重启服务后需重新上传，已有结果仍保留在输出目录。

程序日志：`app/server.log` 和 `app/server-error.log`。

界面源码：`app/web/`；处理服务：`app/studio_server.py`；原始 Gradio 界面保留在 `app/app.py`，不再作为默认首页。

已验证：图片 1448×1086 → 2896×2172；4 帧短视频 640×480 → 1280×960，ProRes MOV 导出及 H.264 浏览器预览。长视频及其他编码未逐一实测。

## 便携包入口

双击 `DLSS Studio.exe` 自动启动服务并打开独立工作台。右上角“浏览器”可访问同一服务；使用浏览器时请保持独立窗口开启，可将它最小化。关闭独立窗口会停止服务。不同页面的当前素材和预览独立保存。

主界面适配窗口高度，常用参数直接显示；高级参数、自定义尺寸、视频导出使用独立面板，小窗口自动分页。窄屏点击“增强设置”切换设置视图。

独立窗口需要 WebView2；缺少时可选择通过包内微软引导组件联网安装。窗口浏览器数据保存在 `desktop-profile/`，服务日志在 `logs/desktop-server.log`。
